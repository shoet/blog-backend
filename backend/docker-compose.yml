version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: ./Dockerfile
      args:
        - target=dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    environment:
      ENV: dev
      PORT: 3000
